# Staging/prod JSON emit from the Backend lane (PR #25).
# Copy into conf/custom.ini (or include) when running Grafana against this Loki path.
# Repo defaults stay console/text — do not commit this as defaults.ini.

[log]
mode = console file
level = info

[log.console]
format = json

[log.file]
format = json
